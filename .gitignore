# caches do Python
__pycache__/
*.pyc

# preferencias pessoais do usuario (geradas pelo programa)
qcs_user_settings.json

# atalhos do Windows (contem caminhos especificos desta maquina)
*.lnk

# configuracao local do Claude Code
.claude/

# ruff linter cache
.ruff_cache/

# crash log written by the app when it runs with no console (pythonw)
sourceCode/QCS_crash.log

# saidas geradas pelo programa (se rodado de dentro do repositorio)
QCS qualified tscp data/
QCS qualified hobo data/
QCS DataView (fixed scale)/
QCS DataView (unfixed scale)/
DatabaseView/
QCS_report.csv
QCS_test_report.csv
QCS_flag_legend.csv
QCS_tscp_stat.csv
QCS_light_window.svg

# arquivos temporarios de trabalho (mensagens de commit/PR, diagnosticos)
_commitmsg.txt
_prbody.md
_diag_*.py
_smoke_test.py
