# Python caches
__pycache__/
*.pyc

# Personal user preferences (generated by the program)
qcs_user_settings.json

# Windows shortcuts (contain machine-specific paths)
*.lnk

# Claude Code local configuration
.claude/

# Ruff linter cache
.ruff_cache/

# Crash log written by the app when it runs with no console (pythonw)
sourceCode/QCS_crash.log

# Program outputs (when the app is run from inside the repository)
*_QLF/
QCS qualified tscp data/
QCS qualified hobo data/
QCS DataView (fixed scale)/
QCS DataView (unfixed scale)/
DatabaseView/
QCS_report.xlsx
QCS_test_report.xlsx
QCS_flag_legend.xlsx
QCS_tscp_stat.xlsx
QCS_combine_method.txt
QCS_light_window.svg
QCS_light_window_*.svg

# Temporary working files (commit/PR messages, diagnostics)
_commitmsg.txt
_prbody.md
_diag_*.py
_smoke_test.py

# Packaging build artifacts (rebuilt at will from packaging/*.spec + .iss)
packaging/dist/
packaging/Output/
packaging/v12_env/
