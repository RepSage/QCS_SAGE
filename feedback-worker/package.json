{
  "name": "qcs-feedback-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
